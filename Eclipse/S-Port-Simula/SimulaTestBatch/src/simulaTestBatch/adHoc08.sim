begin integer MAX, SQRT_MAX;   
  MAX      := 77000;  
     
  begin boolean array bArray( 0 : MAX );   
        integer k, n2;  
       
         n2 := 63001; 
%         outtext("TEST: n2="); outint(n2, 0); outtext("; MAX="); outint(MAX, 0); outimage; 
         for k := n2 step n2 until MAX do begin 
%               outtext("TEST: k="); outint(k, 0); outimage; 
               bArray(k) := false; 
         end;   
  end;  
end 
